build/
release/
ts-client/

# Intellij
.idea/

# macOS
.DS_Store

# vscode
.vscode/

# vim
.*.sw?

# Vue
vue/src/store/generated/
vue/node_modules
vue/dist

# Logs and debug
scripts/automation_scripts/automation_results*
testutil/e2e/protocolLogs/
debugging/
testutil/e2e/logs/

# Misc
scripts/vars/
rpcprovider.yml
rpcconsumer.yml

# Environment variables and generated configs (secrets should not be committed)
.env
.env.local
config/smartrouter_examples/smartrouter_*.yml

# Local TLS cert/key files (do not commit)
protocol/rpcprovider/cert.pem
protocol/rpcprovider/key.pem

# Node modules
**/node_modules

# Pairing lsit
**/pairingList.json

# Reward server ignores
.storage/
badger_test

# Local load-test / adversarial probing fixtures (target lists, bodies, results).
# Must never be committed — the target lists in particular can look like
# reconnaissance data to automated scanners.
attack/

config/health_examples/health_template_gen.yml
# Yarn
.yarn/

**/dist/

# Release notes extracted from CHANGELOG.md by the release workflow
.release-notes/

agent_docs/
/readme-docs/
mock-node-server/
.claude/
Claude.md
lavap
# Built smartrouter binary at the repo root (e.g. from `make build`). Anchored with a leading slash so it
# does NOT ignore the cmd/smartrouter/ source package.
/smartrouter
# Local license key — a credential, must never be committed. Anchored to the repo root.
/license.key
debugging/logs/
