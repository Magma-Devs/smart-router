# Smart Router Direct RPC Configuration
# Testing Phases 1-5: JSON-RPC over HTTP/HTTPS + WebSocket Subscriptions
# Mode: Direct connections to Ethereum RPC endpoints (no Lava providers!)

endpoints:
  - listen-address: "0.0.0.0:3360"
    chain-id: "ETH1"
    api-interface: "jsonrpc"
    network-address: "0.0.0.0:3360"

direct-rpc:
  # HTTP+WS Endpoint 1
  - name: "eth-rpc-1"
    chain-id: "ETH1"
    api-interface: "jsonrpc"
    node-urls:
      - url: "https://lb.drpc.live/ethereum/YOUR_DRPC_TOKEN"
        skip-verifications:
          - chain-id
          - pruning
      - url: "wss://lb.drpc.live/ethereum/YOUR_DRPC_TOKEN"

  # HTTP+WS Endpoint 2
  - name: "eth-rpc-2"
    chain-id: "ETH1"
    api-interface: "jsonrpc"
    node-urls:
      - url: "YOUR_ETH_RPC_PROVIDER_URL"
        skip-verifications:
          - chain-id
          - pruning
      - url: "wss://g.w.lavanet.xyz:443/gateway/eth/rpc/YOUR_LAVA_GATEWAY_KEY"

  # HTTP+WS Endpoint 3
  - name: "eth-rpc-3"
    chain-id: "ETH1"
    api-interface: "jsonrpc"
    node-urls:
      - url: "https://ethereum-rpc.publicnode.com"
        skip-verifications:
          - chain-id
          - pruning
      - url: "wss://ethereum-rpc.publicnode.com"
