release/
build/

vue/src/store/generated/
vue/node_modules/
vue/dist/

scripts/vars/

testutil/e2e/logs/
testutil/debugging/

ecosystem/**/package-lock.json

.github/
.git/
.idea/
.vscode/
.storage/
.vscode/
go.work
go.work.sum
*.md

# All docker build/compose machinery lives here; none of it needs to be
# inside the build context (Dockerfiles are passed via -f / dockerfile:,
# context stays the repo root or goreleaser's staging dir).
docker/
cmd/**/Dockerfile

.dockerignore
.gcloudignore

Makefile